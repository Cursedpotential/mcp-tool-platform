# Legacy Workspace Directory Index & Parsing Assets (`D:\AI_Workspace`)

> **⚠️ GUARDRAIL NOTE:** This document indexes the legacy workspace located on the `D:` drive. Active development MUST remain in `C:\Users\matts\Projects\TheBigOne\MCP_Tool_Platform`. The `D:` drive is strictly a read-only source for mining legacy parsers and logic.

## 1. The Custom Parsers (The "Goldmine")
Found in `D:\AI_Workspace\Projects\TheBigOne\MCP_Tool_Platform\MCP_Tool_Platform_Repo\server\mcp\loaders` and `plugins`:

* **`xml-sms-parser.ts`**: Appears to be the custom handler for the "very very very large XML files" you mentioned (likely SMS Backup & Restore XML exports or similar).
* **`pdf-imessage-parser.ts`**: Appears to handle exported iMessage threads in PDF format.
* **`timeline-parser.ts`**: Likely contains the logic for sorting, linking, and formatting extracted messages into a timeline.
* **`enhanced-xml-chunker.py`** (Found in `01_Timeline_Forensics` archive): Python script likely designed specifically to chunk massive XML message dumps without breaking the XML tree or message boundaries.
* **`chatgpt_parser.py`**: Utility for parsing exported ChatGPT conversations.

*Assumption to verify:* I assume `xml-sms-parser.ts` and `enhanced-xml-chunker.py` are the primary tools we need to port over for Sprint 1 to handle the massive message XML files. Is this correct?

## 2. Evidence Processing & Timeline Generation (`TraceIQ` & `Evidence_Analysis`)
Found in `D:\AI_Workspace\Projects\TheBigOne\TraceIQ` and `Evidence_Analysis`:

* **`timeline-explorer-pro` / `TraceIQ_Main`**: Appears to be a complete UI/frontend for visualizing the timelines generated by the parsers. Contains its own `utils/parser.ts`.
* **`location-admin`**: Likely handles the geospatial/location data associated with messages and photos.
* **`ConflictAnalysisApp`**: Appears to be an early version of the conflict-resolution logic that Semantica will now handle.
* **`forensic-data-refinery`**: Sounds like a data normalization pipeline for turning raw text/evidence into structured rows.

*Assumption to verify:* We probably don't need to port the React UIs right now, but we *should* port the underlying parsing logic from `forensic-data-refinery` into our new LlamaIndex extractor pipeline. Correct?

## 3. Voice Analysis & Context (`Voice_Analysis`)
Found in `D:\AI_Workspace\Projects\TheBigOne\Voice_Analysis`:

* **`Chronicle_Voice_App` / `story-voice-backend`**: Appears to process transcripts, video analysis, and voice-to-text.
* **`Context_Analysis_Suite/Chat_Parser_App`**: Another chat parser specifically built for context extraction.

*Assumption to verify:* We can defer this to Sprint 2 or 3, as our current priority is text messages and PDFs.

## 4. The `Tools` Directory (`D:\AI_Workspace\Tools`)
This directory is massive and contains hundreds of modular utilities:

* **`Chunker`**: Contains `exporters`, `parsers`, and `schemas`. Likely overlaps with `enhanced-xml-chunker.py`.
* **`Chat_Parsers` & `Context_Parsers`**: Dedicated folders for parsing logic (`xml-to-csv-converter`, `xml-stream-processor`, `lexicon-analysis-engine`).
* **`NLP / Manipulative-Expression-Recognition`**: This is **huge**. This looks exactly like the Emotion/Toxicity analysis we were going to use IBM Watson for. If this works locally, we don't need IBM at all.
* **`DirectoryScanner`, `TetherPro`, `EnvManager`**: Utility apps.

*Assumption to verify:* The `Manipulative-Expression-Recognition` and `xml-stream-processor` in the Tools folder seem critical. Should we prioritize bringing these over?

## 5. The `External` Directory (`D:\AI_Workspace\External`)
Contains third-party tools and plugins you've collected:

* **`autopsy_plugins-master`**: A massive collection of forensics plugins for the Autopsy platform (Android-export, GoogleDrive, iOS_sysdiagnose, Plaso, WhatsApp, etc.).
* **`Stirling-PDF`**: Powerful local PDF manipulation tool.
* **`YPA-master` / `OpenBackupExtractor`**: Tools for pulling data from phone backups.

*Assumption to verify:* These are reference/standalone tools. We don't need to port code from here, but we will use them to generate the raw data that feeds into our pipeline.

---
**Next Steps for You:**
Please review the assumptions above. If you tell me which specific parsers (e.g., "Yes, bring over xml-sms-parser and the NLP toxicity analyzer"), I will immediately copy them into the `C:` drive and wrap them in LlamaIndex `BaseReader` classes so we can use them in Sprint 1 today.