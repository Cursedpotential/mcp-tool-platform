---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/typescript-error-fixes/
  - client/components/
  - shared/schemas/
autonomous: true

must_haves:
  truths:
    - TypeScript compiler exits with zero errors
    - No implicit any errors remain
    - No missing type declarations
    - Build process completes successfully
  artifacts:
    - path: "tsc --noEmit output"
      provides: "Zero errors reported"
    - path: "build output"
      provides: "Successful compilation"
  key_links:
    - from: "Type fixes"
      to: "Clean build"
      via: "tsc verification"
---

<objective>
Fix all ~80 TypeScript errors from branch merge to achieve clean compilation.

Purpose: Foundation must compile before any infrastructure work can proceed. Type errors block all downstream development.
Output: Zero TypeScript errors, clean build output
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

Phase 1, Wave 1 — Must complete before storage initialization.

Common error patterns from merge:
- Missing type declarations for new imports
- Implicit any in callback functions
- Type mismatches in tRPC router definitions
- Schema conflicts between competing message definitions
</context>

<tasks>

<task type="auto">
  <name>Fix TypeScript compilation errors</name>
  <files>
    - server/**/*.ts
    - client/**/*.ts
    - shared/**/*.ts
  </files>
  <action>
    Run `tsc --noEmit` to identify all TypeScript errors. Fix in priority order:
    
    1. **Critical (blocks build):**
       - Missing imports or undeclared variables
       - Type conflicts in shared schemas
       - Broken tRPC router definitions
    
    2. **High priority:**
       - Implicit any in function parameters
       - Missing return type annotations
       - Interface/property mismatches
    
    3. **Medium priority:**
       - Strict null check violations
       - Unused variable warnings
    
    Specific fixes needed based on codebase analysis:
    - Consolidate competing message schema definitions (drizzle/schema.ts, production-message-schemas, production-pipeline.ts)
    - Fix tRPC router type issues from commented-out calls
    - Add proper type guards for DuckDB/LanceDB initialization code
    
    DO NOT refactor — only fix type errors. Keep changes minimal.
  </action>
  <verify>
    Run: `npm run typecheck` or `tsc --noEmit`
    Expected: Zero errors, exit code 0
  </verify>
  <done>
    tsc --noEmit exits with 0 errors. Build completes successfully.
  </done>
</task>

</tasks>

<verification>
[ ] tsc --noEmit returns exit code 0
[ ] No red squiggles in VS Code for server/, client/, shared/
[ ] Build command completes without type errors
</verification>

<success_criteria>
- Zero TypeScript errors across entire codebase
- Clean build output
- Foundation ready for storage initialization (Phase 1, Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
