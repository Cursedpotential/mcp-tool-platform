# ============================================================================
# Docker Compose Environment Variables
# Copy this file to .env.docker and fill in your actual values
# ============================================================================

# ----------------------------------------------------------------------------
# Neo4j Configuration
# ----------------------------------------------------------------------------
NEO4J_AUTH=neo4j/your-secure-password-here

# ----------------------------------------------------------------------------
# Supabase Configuration (for Directus + PhotoPrism databases)
# ----------------------------------------------------------------------------
SUPABASE_HOST=your-project-id.supabase.co
SUPABASE_PASSWORD=your-supabase-password
SUPABASE_DATABASE=postgres
SUPABASE_USER=postgres

# ----------------------------------------------------------------------------
# Cloudflare R2 Configuration
# ----------------------------------------------------------------------------
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_BUCKET=salem-forensics
R2_ENDPOINT=https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com

# ----------------------------------------------------------------------------
# Directus Configuration
# ----------------------------------------------------------------------------
DIRECTUS_ADMIN_EMAIL=admin@salem.local
DIRECTUS_ADMIN_PASSWORD=your-secure-password-here
DIRECTUS_KEY=replace-with-random-32-char-key  # Generate: openssl rand -hex 16
DIRECTUS_SECRET=replace-with-random-64-char-secret  # Generate: openssl rand -hex 32
DIRECTUS_PUBLIC_URL=http://localhost:8055

# ----------------------------------------------------------------------------
# PhotoPrism Configuration
# ----------------------------------------------------------------------------
PHOTOPRISM_ADMIN_USER=admin
PHOTOPRISM_ADMIN_PASSWORD=your-secure-password-here

# ----------------------------------------------------------------------------
# Redis Configuration
# ----------------------------------------------------------------------------
REDIS_PASSWORD=your-secure-password-here

# ============================================================================
# Quick Setup Instructions
# ============================================================================
#
# 1. Copy this file:
#    cp .env.docker.example .env.docker
#
# 2. Fill in your actual credentials above
#
# 3. Generate random keys:
#    DIRECTUS_KEY=$(openssl rand -hex 16)
#    DIRECTUS_SECRET=$(openssl rand -hex 32)
#
# 4. Start services:
#    docker-compose --env-file .env.docker up -d
#
# 5. Access services:
#    - Neo4j Browser: http://localhost:7474
#    - Directus Admin: http://localhost:8055
#    - PhotoPrism: http://localhost:2342
#    - Redis: localhost:6379
#
# 6. Initial setup:
#    - Neo4j: Login with neo4j/your-password, install APOC plugin
#    - Directus: Login with admin credentials, configure R2 storage
#    - PhotoPrism: Login with admin credentials, start initial indexing
#
# ============================================================================
